# .env.example
# Copy to .env and fill in real values. Do not commit .env.

# Postgres (docker-compose defaults)
DATABASE_URL=postgresql+psycopg://jeopardy:jeopardy@postgres:5432/jeopardy

# OpenAI (optional; enables LLM answer verification)
# Replace with your real key, e.g. export OPENAI_API_KEY=... or set it in .env
OPENAI_API_KEY=sk-your-openai-api-key-here

# Model selection (optional)
OPENAI_MODEL=gpt-4o-mini

# Base URL (optional; leave default for OpenAI)
OPENAI_BASE_URL=https://api.openai.com/v1
